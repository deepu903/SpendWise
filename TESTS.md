# Tests Documentation

| File | Coverage | How to Run |
|------|----------|------------|
| `tests/audit-engine.test.ts` | Tests the deterministic TypeScript logic of the audit rules engine. Covers: downgrading small teams, cancelling unused tools, warning on minimum seat limits, consolidating premium LLMs, and API spend recommendations. | `npm run test` or `npx vitest run` |

**Automated Tests:** Minimum 5 tests are included in the file above.
